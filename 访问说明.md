# 网站访问链接说明

## 可正常打开的链接（推荐用于分享）

以下链接在浏览器中会**直接打开网页**，不会触发下载：

| 方式 | 链接 | 说明 |
|------|------|------|
| **GitHub Pages** | https://dongjun239212.github.io/My-new-portfolio/ | 已配置自动部署，国内可访问（可能稍慢） |
| **Vercel** | 部署后获得 `https://xxx.vercel.app` | 见下方「一键部署到 Vercel」 |

请用上述链接分享网站，不要使用 OSS 默认域名链接。

---

## 阿里云 OSS 默认域名为何会下载 / 白屏？

使用 **OSS Bucket 自有域名**（如 `xxx.oss-cn-beijing.aliyuncs.com`）访问时，阿里云会强制在响应里加上「下载」相关头，导致：

- 浏览器把 `index.html` 当成**附件下载**，而不是渲染成页面  
- 即使不下载，也可能出现**白屏**（资源路径或策略导致）

这是**阿里云 OSS 的平台限制**，无法通过改上传脚本或对象元数据解决。

**正确用法**：若要用 OSS 做国内加速，需要先完成 **ICP 备案**，再在 OSS 控制台为 Bucket **绑定自定义域名**。通过自定义域名访问时，即可正常打开网页、不会下载。详见 `ALIYUN-OSS-CDN-步骤.md` 第 6、7、8 步。

---

## 一键部署到 Vercel（多一个可分享链接）

1. 打开 https://vercel.com 并登录（可用 GitHub 账号）。
2. 点击 **Add New → Project**，选择你的 GitHub 仓库 `My-new-portfolio`。
3. 构建配置保持默认即可（Vercel 会识别 Next.js）：
   - Build Command: `npm run build`
   - Output Directory: `out`
4. 点击 **Deploy**，等待完成后会得到 `https://你的项目名.vercel.app`。
5. 之后每次推送到 GitHub，Vercel 会自动重新部署。

该链接可正常打开网页，适合作为备用或海外访问。

---

## 加载速度优化

本站已做这些优化以加快首屏显示：

- **字体**：使用 `display: swap`，先显示系统字体再切换为定制字体，避免长时间白屏。
- **首屏图**：Hero 大图标记为高优先级加载（`fetchPriority="high"`）。
- **首屏外图片**：KEY PROJECTS、DESIGN THINKING 等区域图片使用懒加载（`loading="lazy"`），减少首屏请求量。

若 **GitHub Pages 在国内仍感觉慢**，可考虑：

1. **部署到 Vercel**：用上面「一键部署到 Vercel」得到 `xxx.vercel.app` 链接，部分网络下会更快。
2. **国内加速**：完成 **ICP 备案** 后，按 `ALIYUN-OSS-CDN-步骤.md` 为 OSS 绑定自定义域名并配置 CDN，用该域名访问可获得国内节点加速，加载会明显更快。

---

## 总结

- **分享 / 日常访问**：用 **GitHub Pages** 或 **Vercel** 链接。
- **OSS**：仅在上传与测试时使用；要「正常打开网页」需**绑定已备案的自定义域名**后再用该域名访问。
